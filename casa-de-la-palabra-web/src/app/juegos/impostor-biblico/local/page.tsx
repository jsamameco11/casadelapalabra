import { LocalGame } from "@/components/impostor/local-game";

export const metadata = { title: "El Impostor Bíblico — Un solo celular" };

export default function ImpostorLocalPage() {
  return <LocalGame />;
}
