import { CreateRoomForm } from "@/components/impostor/create-room-form";

export const metadata = { title: "Crear sala — El Impostor Bíblico" };

export default function CrearSalaPage() {
  return <CreateRoomForm />;
}
