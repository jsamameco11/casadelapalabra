import { JoinRoomForm } from "@/components/impostor/join-room-form";

export const metadata = { title: "Entrar a sala — El Impostor Bíblico" };

export default function EntrarSalaPage() {
  return <JoinRoomForm />;
}
